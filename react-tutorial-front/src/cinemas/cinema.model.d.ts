export interface cinema {
    id: number;
    name: string;
  }
  export interface cinemaCreationDTO {
    name: string;
    latitude?: number;
    longitude?: number;
  }
  
  export interface landingPageDTO {
  }